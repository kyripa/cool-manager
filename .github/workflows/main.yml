name: CI

on: 
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: [windows-latest]

    steps:
    - uses: actions/checkout@v2
    - name: Display the path
      run:  powershell .\ps2exe.ps1 -iconFile "CoolManage.ico" -inputFile "CoolManage.ps1" -outputFile "CoolManage.exe" -requireAdmin -copyright "Andrew Kuripka" -noOutput -noError -noconsole -verbose
      shell: powershell  
    - name: Upload
      uses: "marvinpinto/action-automatic-releases@latest"
      with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: true
          title: "Development Build-${{ env.GITHUB_RUN_ID }}"
          files: "CoolManage.exe" 
 
